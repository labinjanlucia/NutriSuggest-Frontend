# NutriSuggest Frontend Environment Configuration
# Configuration for frontend deployment

# Frontend Port Configuration
FRONTEND_PORT=80

# API Endpoints - Configure these to point to your backend services
# For Docker containers: use host.docker.internal to access host machine
# For development: use localhost
# For remote backend: use the backend server's IP/domain
VITE_API_URL=http://host.docker.internal:3000/api
VITE_RECOMMENDATION_API_URL=http://host.docker.internal:8000/api

# Application Configuration
VITE_APP_NAME=NutriSuggest
VITE_APP_VERSION=1.0.0

# Nginx Configuration
NGINX_HOST=localhost
NGINX_PORT=80